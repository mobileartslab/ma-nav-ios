import SwiftUI

struct MainView: View {
  var body: some View { 
    VStack {
      Text("Main View")
        .font(.largeTitle)
        .fontWeight(.semibold)
        .padding(.bottom, 20)
    }
  }
}
